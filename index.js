const numero1 = 300;
const numero2 = 400;

const soma = (a, b) => {
    return a + b;
};

const subtrair = (a, b) => {
    return a - b;
};

const multiplicar = (a, b) => {
    return a * b;
};

const valorTotal = soma(numero1, numero2);
console.log(`O valor total é ${valorTotal}!`);